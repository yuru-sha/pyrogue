# PyRogue 0.3.1 仕様書

## 1. 目的

PyRogue を、Rogue 5.4 系のゲーム体験に近い、文字表示のローグライクとして再構成する。
既存のゲームを一度に遊べる状態へ保ちつつ、ゲーム状態と表示変換を分離し、将来のタイル表示を可能にする。

## 2. 仕様の優先順位

仕様の優先順位は次のとおりとする。

1. 本書
2. Rogue 5.4 系の公開ソースコードおよびマニュアル
3. 既存コードと既存ドキュメント

原作資料と実装の細部が一致しない場合は原作資料を優先する。資料間で確定できない値は、採用値と理由を本書または関連ドキュメントに記録する。ゲーム内設定を増やして複数のルールセットを提供してはならない。

参照資料:

- [Rogue 5.4.4 source archive](https://github.com/Davidslv/rogue)
- [BSD Rogue 5.4 source](https://github.com/lcn2/rogue5.4)
- [Rogue 5.4.4 documentation](https://github.com/kngwyu/rogue5.4.4)

## 3. リリース方針

- 対象バージョン: `0.3.1`
- `pyproject.toml` と `src/pyrogue/__init__.py` のバージョンを一致させる
- 既存セーブとの互換性は保証しない
- `GAME_VERSION` は保存形式の互換性版として管理し、保存形式変更時に更新する
- コミット、ブランチ、プッシュ、PR は本仕様の対象外とする

## 4. ゲームの完了条件

プレイヤーはダンジョンを探索し、26階でイェンダーのアミュレットを取得した後、地上へ帰還して勝利する。HP が尽きた場合はパーマデスとしてゲームオーバーになる。

必須の進行:

```text
ゲーム開始 → 1階から探索 → 階段を降りる → 26階でアミュレット取得
→ 階段を上って帰還 → 勝利
```

## 5. 必須ゲーム要素

### 5.1 ダンジョン

- 全 26 階を生成する
- 通常階は部屋、通路、扉、階段を持つ
- 通常階は原作同様の3×3区画から部屋欠落、部屋内迷路、暗室を生成し、部屋をグラフ状の通路で接続する。固定階を全面迷路にしない
- プレイヤーの開始位置と次階への階段を必ず配置する
- 到達不能な必須地点を生成しない
- 視界外の地形・エンティティは表示しない
- 一度見た地形は探索済みとして記憶できる
- 階層ごとの状態は、階段移動後も正しく復元できる
- 未発見の隠し扉と隠し通路は階層状態に保存し、表示時は壁・空白として描画して通行不可とする。`s` は周囲8セルを Rogue 5.4.4 の確率で探索する（扉は1/5、罠は1/2、通路は1/3。幻覚で分母に3、盲目で2を加算）。探索の指輪は各ターン後に同じ探索を行う。
- 隠し扉は接続する通常部屋の出口に生成し、`1d10 + 1 < 階層番号` の場合に限り1/5で隠す。通路は `1d10 + 1 < 階層番号` の場合に限り、各通路セルを1/40で未発見状態にする。いずれもGameStateのseed付きRNGを使う。

通常のFOV表示を維持しつつ、GUIではTabで一時的にFOV表示を無効化できる。無効化中は
表示用に全地形・エンティティを描画するが、探索済み状態は更新しない。

現在の BSP、迷路、部屋・通路生成コードは、原作仕様と照合したうえで再利用または削除する。原作にない生成バリエーションを、拡張性を理由に残してはならない。

### 5.2 プレイヤーと戦闘

- プレイヤーは経験値、レベル、HP、攻撃、防御、武器、防具を持つ
- 戦闘はターン制とする
- 隣接した敵への攻撃、敵からの攻撃、死亡を実装する
- 命中、ダメージ、経験値、レベルアップ、装備補正は Rogue 5.4 系の仕様に合わせる
- 投擲は武器ごとのダメージと適合するランチャーの補正を適用する
- 現行の独自クリティカル、単純な攻撃力-防御力計算、独自の飢餓補正などは原作仕様へ置換する
- 戦闘結果と状態変化をメッセージで通知する

### 5.3 モンスター

- Rogue 5.4 系の主要モンスターを実装する
- モンスターごとの表示記号、能力、経験値、出現条件、行動を原作資料に合わせる
- プレイヤーを認識して追跡・攻撃する
- 視界外のモンスター情報を不正に表示しない
- モンスターの行動はプレイヤーの行動後に処理する。階段で新規生成した階へ移動したターンは到着階の敵を動かさず、保存済み階では到着ターンも敵を処理する

### 5.4 アイテム

次のカテゴリを実装対象とする。

- 武器
- 防具
- 食料
- 薬
- 巻物
- 杖
- 指輪
- 金貨
- イェンダーのアミュレット

各アイテムは、配置、取得、所持、使用、装備、解除、投擲、効果、識別状態を持つ。未識別アイテムは種類ごとの不確定な名称で表示し、使用または識別によって正体を確定する。

呪われた装備は解除を制限する。識別、呪い、アイテム効果、生成確率は Rogue 5.4 系の資料を基準にする。
種類別の名前と出現重みも原作に合わせる。カテゴリ重みは薬26、巻物36、食料16、武器7、防具7、指輪4、杖4とする。
新しい階では通常アイテムの配置を9回試行し、各試行を36%で成功させる。各部屋には50%で金貨を置き、金貨がある部屋では80%、ない部屋では25%でモンスターを1体配置する。通常アイテムと金貨は空いている通常床へ置き、階段を避ける。部屋のモンスターはその部屋の床に配置する。5%で宝物部屋を生成し、同じ部屋の床にアイテム2〜9個と、アイテム数に応じたモンスターを配置する。トラップは10段階乱数が階層番号未満のとき出現し（階層1で10%、階層10以深で100%）、出現数は1〜max(1, 階層番号//4)体から選び最大10体とする。通常床に置き（部屋迷路と階段を除く）、階層1〜3では常に1体とする。食料が3階続けて生成されない場合は次階で食料を強制する。
開始時は食料1個、リングメイル、+1メイス、+1ショートボウ、矢25〜39本を持つ。通常食料は `HUNGERTIME - 200 + rnd(400)`（`HUNGERTIME=1300`、`rnd(400)` は0〜399）を回復し、イェンダーのアミュレットは最深階に生成する。

canonical `GameState` が適用する主な効果は次のとおりとする。

| 種別 | 効果 | 状態変化 |
| --- | --- | --- |
| 巻物 | identify | 所持中の薬、巻物、指輪、杖を識別する |
| 巻物 | identify potion / weapon / armor / ring, wand or staff | 選択した該当カテゴリのアイテム1個を識別する |
| 巻物 | remove curse | 所持品の呪いを解除する |
| 巻物 | enchant weapon / armor | 装備中の武器は命中またはダメージ補正の一方を +1、防具は保護値を +1 し、呪いを解除する |
| 巻物 | light | 現在の部屋（通路では可視範囲）を探索済みにする |
| 巻物 | teleportation | 同じ階の空いた歩行可能セルへ移動する |
| 巻物 | magic mapping | 現在の階全体を探索済みにする |
| 巻物 | monster confusion / hold monster / sleep / scare / aggravate | モンスターの混乱、拘束、睡眠、退散、覚醒を適用する |
| 巻物 | food detection / create monster / protect armor | 食料位置を示す / モンスターを生成する / 防具を保護する |
| 杖 | damage | 方向上の最初のモンスターに magic missile は 1d4、それ以外は 6d6 のダメージを与える |
| 杖 | light | 現在の部屋を照らす。方向は要求しない |
| 杖 | teleport monster | 方向上の最初のモンスターを同じ階の空いたセルへ移動する |
| 杖 | invisibility / polymorph / haste / slow / drain life / cancellation | 対象モンスターの状態またはHPに原作系効果を適用する |
| 杖 | teleport away / teleport to / nothing | 対象を移動する、または効果なしとする |
| 薬 | poison / strength / restore strength / raise level | 強さまたはプレイヤーレベルを変化させる |
| 薬 | confusion / hallucination / blindness / see invisible / levitation / haste | 対応する一時状態を付与する |
| 薬 | healing / extra healing / monster detection / magic detection | HPを回復する、または該当するアイテム・モンスターを示す |
| 指輪 | protection / add strength / dexterity / increase damage | 防御値 / 強さ由来の命中・ダメージ / 装備中の武器の命中・ダメージに補正値を反映する |
| 指輪 | sustain strength | 毒矢と rattlesnake による強さ低下を防ぐ |
| 指輪 | searching | 各ターンに隣接する罠を自動発見する |
| 指輪 | regeneration | 装備中の指輪1個につき、ターンごとにHPを1回復する |
| 指輪 | see invisible / stealth / slow digestion / teleportation / aggravate / maintain armor | 原作に対応する視認、飢え、移動、覚醒、防具保護を適用する。stealth は mean モンスターの覚醒を2/3の確率で抑える |
| 薬 | levitation | 30ターン浮遊し、罠を無効化し、mean モンスターの覚醒を2/3の確率で抑える |

杖・薬・巻物・指輪の未識別表示は種類ごとに一意な見た目を割り当て、効果を知ると同じ効果のアイテムを識別する。杖は光のみ10〜19チャージ、その他は3〜7チャージで生成する。武器・防具・指輪の呪いと補正も原作の確率に従う。従来の「light scroll」は互換用の効果名として受け付けるが、生成対象には含めない。

アイテムの使用は、失敗した対象指定を除き1ターンを消費する。巻物は使用後に
インベントリから消費し、杖は有効な方向指定でチャージを1消費する。乱数を伴う効果は
`GameState` 所有のseed付きRNGだけを使用する。

### 5.5 食料

- 食料を取得して食べられる。食事時に負の空腹値は0へ戻し、`HUNGERTIME - 200 + rnd(400)`（`rnd(400)` は0〜399）を加えて `STOMACHSIZE` を上限とし、空腹状態を解除する。
- 通常食料（food ration）は30%の確率で悪い味となり、経験値を1加算してレベルアップ判定を行う。slime mold は原作の果物（`o_which == 1`）として悪い味判定を行わない。結果はseed付きRNGで決定する。
- 食料はターン経過に伴うリソースとして扱う
- 空腹、飢餓、餓死の挙動は Rogue 5.4 系に合わせる
- 現行の5段階状態、攻撃・防御ペナルティ、満腹ボーナスはそのまま維持しない

### 5.6 罠

- 原作に存在する罠を実装する
- 罠の発見、発動、効果を原作資料に合わせる
- 原作にない解除難易度や解除専用システムは追加しない

罠は踏んだ時点で発見済みになる。踏まれても罠そのものは階層上に残り、再び踏めば再発動する。罠の効果は次のとおりとする。

| 罠 | Rogue 5.4.4 の効果 |
| --- | --- |
| trapdoor | 次の階へ移動する。最深階では移動しない。発見済みの罠は元の階層に残る |
| bear trap | `BEARTIME`（`spread(3)`、3回の移動試行）の間、移動を妨げる。ダメージは与えない。拘束中の移動試行は通常どおり1ターンを消費し、他コマンドは通常どおり実行できる（移動拘束は解除されない） |
| teleport | 同じ階の空いた歩行可能セルへ移動する。発見済みの罠は元の位置に残る |
| poison dart | 命中判定 `swing(level + 1, armor_class, 1)` に成功した場合のみ1d4ダメージ。生存時、sustain strength 装備中または `save(VS_POISON)` 成功なら強さ低下なし。それ以外は強さを1失う。外れた場合はダメージ・強さ低下なし |
| sleeping gas | `SLEEPTIME`（`spread(5)`、後続5コマンド）の間、コマンドを実行できない |
| rust | armor が leather でなく、保護効果もない場合に防具を1段階弱める |
| mysterious | 原作の11種の効果メッセージからseed付きで1つを選ぶ |
| arrow | 命中判定 `swing(level - 1, armor_class, 1)` に成功した場合のみ1d6ダメージ。外れた場合はダメージなしで矢1本を足元に落とす |

命中判定はRogueの `rnd(20) + wplus >= 20 - at_lvl - op_arm` とする。矢・毒矢の攻撃時は防具の armor class とプレイヤーレベルを使い、乱数はGameState所有のseed付きRNGを使う。罠の発動で死亡した場合は原作の罠別死因を記録する。

罠の発見状態、bear restraint・sleeping gas の残り時間をセーブ・復元する。セーブには既存要件として、モンスターの最大HP、経験値、追跡状態、種別固有の行動状態、所持品と探索対象、アイテム識別・呼称、一時効果、失神残りターン、および隠し地形も含める。
プレイヤー状態にbear restraintのタイマーを追加するため、保存形式版を `0.3.8` に更新し、`0.3.7` 以前のセーブは互換性検査で拒否する。

### 5.7 勝利・死亡・スコア

- アミュレット取得だけでは勝利にしない
- アミュレット取得後に地上へ帰還した時点で勝利とする
- 勝利・死亡時はスコアと最深到達階を表示する。`current_floor` は現在位置、`deepest_floor` は進行記録として扱う
- スコアは所持金の合計とし、モンスター撃破数やアミュレット所持による固定加点は設けない
- パーマデスを維持し、死亡したゲームを再開できない

## 6. 操作

Rogue 5.4.4 のコマンド意味論を、CLI・GUI 共通の canonical コマンドとして提供する。

- `f` は方向を受け取り、その方向の隣接モンスターだけを攻撃する。対象不在または方向指定の取消しはターンを消費しない。
- `a` は直前の適格なコマンドを再実行する。未実行履歴時は失敗し、`a` 自体、UI表示・オプション・識別などターン非消費コマンドは履歴にしない。
- `c` は所持品を選択して呼称（名前）を設定し、`o` はオプション画面を開く。どちらもターンを消費しない。UI固有の選択・表示はアダプターで行い、ゲーム状態の規則は GameState に置く。
- vi方向の大文字 `HJKLYUBN` はその方向へ走る。走行は道の分岐・障害物・ドア・モンスターで停止し、通常移動と同じ罠・ターン処理を使う。
- 数字プレフィックスは Rogue 5.4.4 の対象コマンドに限り反復し、最大値は255とする。CLIとGUIは同じ反復ルールを使い、死亡・勝利・失敗またはターン非消費で終了する。`a`、`f`、オプション、呼称など対象外コマンドは反復しない。
- CLIとGUIのキー入力は同じ GameState コマンド意味論へ渡す。画面遷移や対象選択だけをUIアダプターが担当する。
- ヘルプとクイックガイドは実装済みのキー意味に一致させ、`o`/`c` を扉操作として記載しない。

既存の自然言語コマンドや独自ショートカットは、原作キーと衝突しない範囲でのみ残す。キーの衝突を別名追加で隠してはならない。特に移動と検索など、同じキーに複数の意味を割り当てない。

## 7. 表示アーキテクチャ

ゲームロジックは TCOD の Console、文字、色、タイル画像を直接扱わない。

表示は次の二段階に分ける。

```text
GameState
  ↓
表示用セルへの変換
  ↓
文字レンダラー（現行）
```

表示用セルは少なくとも次の情報を持つ。

- 地形の論理種別
- 可視・探索済み・未探索の状態
- 表示対象エンティティの論理種別
- 表示優先順位
- 表示位置

文字記号と色は文字レンダラー側の責務とする。将来のタイル表示は同じ表示用セルを入力として利用できる構成にする。ただし、今回タイル描画、画像 ID、アニメーション、タイルセット管理は実装しない。

## 8. 実行と再現性

- 通常の GUI プレイはランダムシードを自動生成する
- CLI 引数でシードを指定できる
- ゲーム内部 API からもシードを指定できる
- ダンジョン生成、配置、戦闘などの乱数は、ゲームが管理する乱数源を通して取得する
- 同じバージョン、同じシード、同じ入力列で同じ結果を再現できる

## 9. セーブ

- JSON 形式を継続利用する
- 新形式には仕様バージョンを含める
- 旧形式は自動移行せず、明示的な互換性エラーとして扱う
- セーブには、ゲーム進行に必要なプレイヤー、階層、マップ、アイテム、モンスター、識別状態、乱数状態を含める
- セーブの `current_floor` は現在位置、プレイヤーの `deepest_floor` は到達した最深階を表す
- パーマデス、勝利済み、死亡済みの状態を改ざんなしに復元できるようにする

## 10. 削除・非対象

原作準拠版のゲーム経路から、次の要素を削除する。関連する未使用コード、画面、テスト、ドキュメントも整理対象とする。

- MP と魔法・スペルシステム
- NPC と会話・取引
- Rogue 5.4原典に存在しない状態異常（麻痺など）
- 自動探索
- 重量制限
- 原作にない独自の戦闘補正、特殊攻撃、逃走・分裂ロジック
- 原作にない特別室、過剰なダンジョン生成バリエーション
ただし、識別、呪い、食料、投擲、杖、薬、巻物、指輪、原作の罠は削除しない。

今回の非対象:

- タイル描画の実装
- 音声、BGM、アニメーション
- ネットワーク、ランキングサーバー、マルチプレイ
- 複数の公式ルールセット
- 旧セーブの移行

## 11. テスト受け入れ条件

- 全テストが新仕様の期待値で実行できる
- 乱数シードにより同一ダンジョンを再現できる
- 26階までの降下と帰還勝利を検証できる
- 死亡、パーマデス、スコア表示を検証できる
- 全アイテムカテゴリの取得・識別・使用または装備を検証できる
- 呪われた装備と食料の主要挙動を検証できる
- モンスターの生成、移動、攻撃、死亡、経験値を検証できる
- 可視性、探索済み地形、表示優先順位を検証できる
- seed付き生成で通常階に隠し扉・隠し通路が配置され、探索前は見えず、探索成功後に地形・通行・視界へ反映される。
- 隣接する隠し扉・通路・罠、および盲目・幻覚による確率修正が、ゲームのseed付きRNGを使いセーブ後も維持される。
- 新旧セーブ形式の扱いを検証できる
- GUI と CLI が同じコマンド処理・ゲームルールを利用する
- トラップの矢・毒矢命中/ミス、ダメージ・毒セーブ、bear restraint、sleeping gas の原作持続時間、発見/残存/矢回収状態をseed付きで検証する。
- 通常食料を負の空腹値、異なる栄養乱数値、満腹上限で食べたとき、空腹値の0への補正・原作栄養値・`STOMACHSIZE` 上限・食事後の空腹解除を検証する。
- 通常食料の悪い味（30%）と良い味（70%）をseed付きで検証し、悪い味時の経験値+1とレベルアップ判定、および果物が悪い味判定を受けないことを検証する。

## 12. ドキュメント整備

実装完了時に、少なくとも次を本仕様と矛盾しない内容へ更新する。

- `README.md`
- `README.ja.md`
- `docs/game_design_document.md`
- `docs/architecture.md`
- `docs/features.md`
- `docs/overview.md`
- `docs/development.md`

Codex の作業規約は、別途ルートの `AGENTS.md` に記載する。本書はゲーム仕様を扱い、エージェントの操作規約を重複して持たない。

## 13. 実装順序

変更は次の縦切りで進め、各段階でゲームまたはテストが実行可能な状態を保つ。

1. 原作仕様値の整理とルール基盤
2. ダンジョン生成・階層進行
3. プレイヤー、モンスター、戦闘
4. 原作アイテム、識別、呪い、食料、罠
5. Rogue 操作体系
6. 表示用セルと文字レンダラー
7. セーブ・勝利・死亡・スコア
8. テスト、README、設計ドキュメント、不要コードの削除

全面的な抽象化や新しいフレームワークは導入しない。既存の Python、TCOD、JSON、テスト基盤を優先して利用する。
