# PyRogue

PyRogueは、Rogue 5.4を参考にしたPython 3.12製の小さなローグライクゲームです。
0.3.0では、seedを指定できる決定的なゲーム状態と、CLI/TCOD GUIで共有する
テキストセル描画を採用しています。

## 起動

```bash
uv sync --locked --extra dev
uv run --locked game --cli --seed 1234
uv run --locked game --seed 1234
```

`--seed`を省略すると自動でseedを生成します。同じバージョン、seed、コマンド列
なら同じゲーム状態になります。

## ルール概要

- 26階。通常の部屋・通路階と、7・13・19階の迷路階。
- 視界、探索済みマップ、到達可能な階段、ターン制戦闘、パーマデス。
- Rogue風のA-Zモンスター、原作系トラップ、飢え、食料、装備、ポーション、巻物、杖、指輪、ゴールド、Amulet。
- Amuletを持って地上へ戻ると勝利。26階に着くだけでは勝利になりません。
- 死亡したゲームは再開できません。最終画面にはスコア、最深階、死因を表示します。
- `current_floor`はプレイヤーの現在位置、`player.deepest_floor`は到達記録です。勝利・死亡サマリーには後者を表示します。
- CLIとGUIは同じ死亡後処理を使い、死亡時だけ現在のセーブを削除します。勝利時は削除しません。

移動はviキー（`h`、`j`、`k`、`l`、`y`、`u`、`b`、`n`）で、`.`は待機です。
完全なコマンド一覧はゲーム中に`?`で確認できます。
TCOD GUIではTabでFOV表示を一時的に無効化して全体を確認できます。再度押すと通常のFOV表示に戻り、探索済みマップの記録は変わりません。

## アーキテクチャ

canonical経路は次の小さな流れです。

```text
GameState -> DisplayCell -> CLI / GameRenderer
       ^          ^
       +-- コマンド / JSONセーブ
```

`GameState`が乱数、階層、エンティティ、インベントリ、戦闘、飢え、視界、終了状態を
所有します。CLIとTCOD rendererは同じ`DisplayCell`を文字と色へ変換します。JSONセーブにはバージョン、
全状態、乱数状態、死亡・勝利状態、パーマデス情報を含め、非互換バージョンは明示的に拒否します。

## 開発

```bash
make verify
```

仕様の正本は[SPEC.md](SPEC.md)（0.3.0）です。現在の文書一覧は[docs/README.md](docs/README.md)を
参照してください。
